<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.jobtimer">

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.JobTimer">
        <activity android:name=".activities.LoginActivity"
                android:theme="@style/Theme.JobTimer.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activities.MainActivity"></activity>
        <activity android:name=".activities.TimerActivity"></activity>
        <activity android:name=".activities.ManageUsersActivity"></activity>
        <activity android:name=".activities.ManageTasksActivity"></activity>
        <activity android:name=".activities.ReferenceActivity"></activity>
    </application>

</manifest>